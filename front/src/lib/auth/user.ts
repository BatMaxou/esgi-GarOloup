export const userConfig = {
  additionalFields: {
    token: {
      type: "string",
      returned: true,
      required: false
    },
    refreshToken: {
      type: "string",
      returned: true,
      required: false
    }
  }
}
