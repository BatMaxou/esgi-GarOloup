vich_uploader:
    db_driver: orm
    mappings:
        role_picture:
            uri_prefix: '%env(string:PUBLIC_UPLOADS_PATH)%/roles/pictures'
            upload_destination: '%kernel.project_dir%%env(string:SSR_PUBLIC_UPLOADS_PATH)%/roles/pictures'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
