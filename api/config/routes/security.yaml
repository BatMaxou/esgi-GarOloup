_security_logout:
    resource: security.route_loader.logout
    type: service

api_login:
    path: /api/login
    methods: ["POST"]

api_refresh_token:
    path: /api/token/refresh
    methods: ["POST"]
