services:
  web:
    ports:
      - "8000:80"

  node:
    ports:
      - "3000:3000"

  db:
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: garoloup

  mailbox:
    ports:
      - "8025:8025"
